# Archived C++ Verification Cases

The C++ verification executables that used to live in this directory have been removed from the main branch.

Active verification now lives in:

- `tests/verification/test_python_beam_benchmarks.py`
- `tests/verification/test_python_dynamic_benchmarks.py`
- `tests/integration/test_gravity_prestress.py`

The old verification intent is now carried by those Orchard FEM tests and by the migration ledger in `docs/cpp_migration_inventory.md`.
