# Archived C++ Sources

The active C++ source files have been removed from the main branch.
`src/` now remains only as an archive marker for the retired solver implementation.

The live solver implementation sits in `orchard_fem/` and should receive all new feature work.
